# Amadeus Self-Service API — https://developers.amadeus.com
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
AMADEUS_HOSTNAME=test            # "test" or "production"

# Supabase — project settings → API
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Telegram — create bot via @BotFather, get chat id with @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Frontend (Vercel env vars use the same SUPABASE_URL / SUPABASE_SERVICE_KEY
# for the server actions; no anon key needed in v1).
